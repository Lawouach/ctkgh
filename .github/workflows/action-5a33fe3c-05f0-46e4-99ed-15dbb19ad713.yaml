name: Reliably Plan [5a33fe3c-05f0-46e4-99ed-15dbb19ad713]

on:
  push:
    paths:
      - "plans/5a33fe3c-05f0-46e4-99ed-15dbb19ad713/**"
    branches:
    - main

jobs:
  run-experiment:
    runs-on: ubuntu-latest
    environment: myenv
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    - name: Install dependencies
      run: |
        python -m pip install --prefer-binary -q -U pip setuptools wheel
        pip install --prefer-binary -U chaostoolkit
        if [[ -f requirements.txt ]]; then
            pip install --prefer-binary --no-cache-dir -r requirements.txt
        fi
    - name: Run experiment
      working-directory: ./plans/5a33fe3c-05f0-46e4-99ed-15dbb19ad713
      env:
        RELIABLY_HOST: app.reliably.dev
        RELIABLY_TOKEN: ${{ secrets.RELIABLY_TOKEN }}
        AWS_REGION: ${{ secrets.AWS_REGION }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        OTEL_EXPORTER_OTLP_TRACES_HEADERS: ${{ secrets.OTEL_EXPORTER_OTLP_TRACES_HEADERS }}
        SLACK_TOKEN: ${{ secrets.SLACK_TOKEN }}
      run : |
        export PATH=$PATH:../../bin
        chaos run             --rollback-strategy=always             --journal-path=journal-20220922093747.json             experiment.json
    - name: Store results
      working-directory: ./plans/5a33fe3c-05f0-46e4-99ed-15dbb19ad713
      run: |
        mkdir 20220922093747
        mv chaostoolkit.log 20220922093747/chaostoolkit.log
        mv journal-20220922093747.json 20220922093747/journal.json
        git config user.name "${GITHUB_ACTOR}"
        git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
        git add 20220922093747
        git commit -s -m "Plan 5a33fe3c-05f0-46e4-99ed-15dbb19ad713 [20220922093747]"
        git pull --rebase origin main
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
